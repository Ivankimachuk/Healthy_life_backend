const validateBody = require("./validateBody");
// const isValidId = require("./isValidId");
const authenticate = require("./authenticate");
const isValidIdWater = require('./isValidWater');
const uploadCloude = require("./uploadCloude");

module.exports = {
  validateBody,
  // isValidId,
  authenticate,
  isValidIdWater,
  uploadCloude,
};
